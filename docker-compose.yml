services:
  reverse-proxy:
    image: nginx:1.27-alpine
    depends_on:
      challenge01:
        condition: service_healthy
      challenge02:
        condition: service_healthy
      challenge03:
        condition: service_healthy
      challenge04:
        condition: service_healthy
      challenge05:
        condition: service_healthy
      challenge06:
        condition: service_healthy
      challenge07:
        condition: service_healthy
      challenge08:
        condition: service_healthy
      challenge09:
        condition: service_healthy
      challenge10:
        condition: service_healthy
    ports:
      - "${REVERSE_PROXY_PORT:-8180}:80"
    volumes:
      - ./reverse-proxy/nginx.conf:/etc/nginx/conf.d/default.conf:ro
      - ./reverse-proxy/index.html:/usr/share/nginx/html/index.html:ro
    restart: unless-stopped

  challenge01:
    build:
      context: .
      dockerfile: challenges/01-travel-desk/Dockerfile
    env_file: .env
    environment:
      PORT: "8101"
      DATA_DIR: /tmp/data
      LLM_PROVIDER: ${LLM_PROVIDER:-deepseek}
      CHALLENGE_FLAG: AICTF{prompt_policies_are_not_entitlement_checks}
      ADMIN_TOKEN: ${ADMIN_TOKEN:-local-admin}
      RATE_LIMIT_PER_MINUTE: ${RATE_LIMIT_PER_MINUTE:-15}
    ports: ["8101:8101"]
    tmpfs: ["/tmp"]
    read_only: true
    security_opt: ["no-new-privileges:true"]
    cap_drop: ["ALL"]
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python3", "-c", "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8101/health').read()"]
      interval: 20s
      timeout: 5s
      retries: 3

  challenge02:
    build:
      context: .
      dockerfile: challenges/02-support-secrets/Dockerfile
    env_file: .env
    environment:
      PORT: "8102"
      DATA_DIR: /tmp/data
      LLM_PROVIDER: ${LLM_PROVIDER:-deepseek}
      CHALLENGE_FLAG: AICTF{secrets_in_context_are_still_secrets_exposed}
      ADMIN_TOKEN: ${ADMIN_TOKEN:-local-admin}
      RATE_LIMIT_PER_MINUTE: ${RATE_LIMIT_PER_MINUTE:-15}
    ports: ["8102:8102"]
    tmpfs: ["/tmp"]
    read_only: true
    security_opt: ["no-new-privileges:true"]
    cap_drop: ["ALL"]
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python3", "-c", "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8102/health').read()"]
      interval: 20s
      timeout: 5s
      retries: 3

  challenge03:
    build:
      context: .
      dockerfile: challenges/03-memory-lane/Dockerfile
    env_file: .env
    environment:
      PORT: "8103"
      DATA_DIR: /tmp/data
      LLM_PROVIDER: ${LLM_PROVIDER:-deepseek}
      CHALLENGE_FLAG: AICTF{conversation_history_becomes_a_trust_boundary}
      ADMIN_TOKEN: ${ADMIN_TOKEN:-local-admin}
      RATE_LIMIT_PER_MINUTE: ${RATE_LIMIT_PER_MINUTE:-15}
    ports: ["8103:8103"]
    tmpfs: ["/tmp"]
    read_only: true
    security_opt: ["no-new-privileges:true"]
    cap_drop: ["ALL"]
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python3", "-c", "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8103/health').read()"]
      interval: 20s
      timeout: 5s
      retries: 3

  challenge04:
    build:
      context: .
      dockerfile: challenges/04-docu-scan/Dockerfile
    env_file: .env
    environment:
      PORT: "8104"
      DATA_DIR: /tmp/data
      LLM_PROVIDER: ${LLM_PROVIDER:-deepseek}
      CHALLENGE_FLAG: AICTF{documents_are_data_until_llms_treat_them_as_instructions}
      ADMIN_TOKEN: ${ADMIN_TOKEN:-local-admin}
      RATE_LIMIT_PER_MINUTE: ${RATE_LIMIT_PER_MINUTE:-12}
    ports: ["8104:8104"]
    tmpfs: ["/tmp"]
    read_only: true
    security_opt: ["no-new-privileges:true"]
    cap_drop: ["ALL"]
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python3", "-c", "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8104/health').read()"]
      interval: 20s
      timeout: 5s
      retries: 3

  challenge05:
    build:
      context: .
      dockerfile: challenges/05-orderly/Dockerfile
    env_file: .env
    environment:
      PORT: "8105"
      DATA_DIR: /tmp/data
      LLM_PROVIDER: ${LLM_PROVIDER:-deepseek}
      CHALLENGE_FLAG: AICTF{models_are_not_access_control}
      ADMIN_TOKEN: ${ADMIN_TOKEN:-local-admin}
      RATE_LIMIT_PER_MINUTE: ${RATE_LIMIT_PER_MINUTE:-14}
    ports: ["8105:8105"]
    tmpfs: ["/tmp"]
    read_only: true
    security_opt: ["no-new-privileges:true"]
    cap_drop: ["ALL"]
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python3", "-c", "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8105/health').read()"]
      interval: 20s
      timeout: 5s
      retries: 3

  challenge06:
    build:
      context: .
      dockerfile: challenges/06-nimbus-hr/Dockerfile
    env_file: .env
    environment:
      PORT: "8106"
      DATA_DIR: /tmp/data
      LLM_PROVIDER: ${LLM_PROVIDER:-deepseek}
      CHALLENGE_FLAG: AICTF{retrieval_needs_provenance_not_hope}
      ADMIN_TOKEN: ${ADMIN_TOKEN:-local-admin}
      RATE_LIMIT_PER_MINUTE: ${RATE_LIMIT_PER_MINUTE:-12}
    ports: ["8106:8106"]
    tmpfs: ["/tmp"]
    read_only: true
    security_opt: ["no-new-privileges:true"]
    cap_drop: ["ALL"]
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python3", "-c", "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8106/health').read()"]
      interval: 20s
      timeout: 5s
      retries: 3

  challenge07:
    build:
      context: .
      dockerfile: challenges/07-helphub/Dockerfile
    env_file: .env
    environment:
      PORT: "8107"
      DATA_DIR: /tmp/data
      LLM_PROVIDER: ${LLM_PROVIDER:-deepseek}
      CHALLENGE_FLAG: AICTF{stored_input_can_cross_roles_and_workflows}
      ADMIN_TOKEN: ${ADMIN_TOKEN:-local-admin}
      RATE_LIMIT_PER_MINUTE: ${RATE_LIMIT_PER_MINUTE:-12}
    ports: ["8107:8107"]
    tmpfs: ["/tmp"]
    read_only: true
    security_opt: ["no-new-privileges:true"]
    cap_drop: ["ALL"]
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python3", "-c", "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8107/health').read()"]
      interval: 20s
      timeout: 5s
      retries: 3

  challenge08:
    build:
      context: .
      dockerfile: challenges/08-atlas-analytics/Dockerfile
    env_file: .env
    environment:
      PORT: "8108"
      DATA_DIR: /tmp/data
      LLM_PROVIDER: ${LLM_PROVIDER:-deepseek}
      CHALLENGE_FLAG: AICTF{ranking_is_a_security_control_when_trust_is_mixed}
      ADMIN_TOKEN: ${ADMIN_TOKEN:-local-admin}
      RATE_LIMIT_PER_MINUTE: ${RATE_LIMIT_PER_MINUTE:-12}
    ports: ["8108:8108"]
    tmpfs: ["/tmp"]
    read_only: true
    security_opt: ["no-new-privileges:true"]
    cap_drop: ["ALL"]
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python3", "-c", "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8108/health').read()"]
      interval: 20s
      timeout: 5s
      retries: 3

  challenge09:
    build:
      context: .
      dockerfile: challenges/09-patchpanel/Dockerfile
    env_file: .env
    environment:
      PORT: "8109"
      DATA_DIR: /tmp/data
      LLM_PROVIDER: ${LLM_PROVIDER:-deepseek}
      CHALLENGE_FLAG: AICTF{safe_tools_can_still_chain_into_unsafe_outcomes}
      ADMIN_TOKEN: ${ADMIN_TOKEN:-local-admin}
      RATE_LIMIT_PER_MINUTE: ${RATE_LIMIT_PER_MINUTE:-12}
    ports: ["8109:8109"]
    tmpfs: ["/tmp"]
    read_only: true
    security_opt: ["no-new-privileges:true"]
    cap_drop: ["ALL"]
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python3", "-c", "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8109/health').read()"]
      interval: 20s
      timeout: 5s
      retries: 3

  challenge10:
    build:
      context: .
      dockerfile: challenges/10-meridian-research/Dockerfile
    env_file: .env
    environment:
      PORT: "8110"
      DATA_DIR: /tmp/data
      LLM_PROVIDER: ${LLM_PROVIDER:-deepseek}
      CHALLENGE_FLAG: AICTF{retrieval_poisoning_plus_tool_trust_breaks_workflows}
      ADMIN_TOKEN: ${ADMIN_TOKEN:-local-admin}
      RATE_LIMIT_PER_MINUTE: ${RATE_LIMIT_PER_MINUTE:-10}
    ports: ["8110:8110"]
    tmpfs: ["/tmp"]
    read_only: true
    security_opt: ["no-new-privileges:true"]
    cap_drop: ["ALL"]
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python3", "-c", "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8110/health').read()"]
      interval: 20s
      timeout: 5s
      retries: 3

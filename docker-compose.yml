services:
  reverse-proxy:
    image: nginx:1.27-alpine
    depends_on:
      challenge01:
        condition: service_healthy
      challenge02:
        condition: service_healthy
      challenge03:
        condition: service_healthy
      challenge04:
        condition: service_healthy
      challenge05:
        condition: service_healthy
      challenge06:
        condition: service_healthy
      challenge07:
        condition: service_healthy
      challenge08:
        condition: service_healthy
      challenge09:
        condition: service_healthy
      challenge10:
        condition: service_healthy
      challenge11:
        condition: service_healthy
    ports:
      - "${REVERSE_PROXY_PORT:-8180}:80"
    volumes:
      - ./reverse-proxy/nginx.conf:/etc/nginx/conf.d/default.conf:ro
      - ./reverse-proxy/index.html:/usr/share/nginx/html/index.html:ro
      - ./AI-HACKING-1.png:/usr/share/nginx/html/AI-HACKING-1.png:ro
    restart: unless-stopped

  challenge01:
    build:
      context: .
      dockerfile: challenges/01-travel-desk/Dockerfile
    env_file: .env
    environment:
      PORT: "8101"
      DATA_DIR: /tmp/data
      LLM_PROVIDER: ${LLM_PROVIDER:-deepseek}
      CHALLENGE_FLAG: MSP{e4sy_d1r3ct_1nj3ct10n}
      ADMIN_TOKEN: ${ADMIN_TOKEN:-local-admin}
      RATE_LIMIT_PER_MINUTE: ${RATE_LIMIT_PER_MINUTE:-15}
      RELOAD: ${RELOAD:-true}
      PYTHONPATH: /app/challenge:/app
    ports: ["8101:8101"]
    volumes:
      - ./shared:/app/shared
      - ./challenges/01-travel-desk:/app/challenge
    tmpfs: ["/tmp"]
    read_only: true
    security_opt: ["no-new-privileges:true"]
    cap_drop: ["ALL"]
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python3", "-c", "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8101/health').read()"]
      interval: 20s
      timeout: 5s
      retries: 3

  challenge02:
    build:
      context: .
      dockerfile: challenges/02-support-secrets/Dockerfile
    env_file: .env
    environment:
      PORT: "8102"
      DATA_DIR: /tmp/data
      LLM_PROVIDER: ${LLM_PROVIDER:-deepseek}
      CHALLENGE_FLAG: MSP{34sy_S3nsi+iv3_Inf0rm4+i0n_Discl0sur3}
      ADMIN_TOKEN: ${ADMIN_TOKEN:-local-admin}
      RATE_LIMIT_PER_MINUTE: ${RATE_LIMIT_PER_MINUTE:-15}
      RELOAD: ${RELOAD:-true}
      PYTHONPATH: /app/challenge:/app
    ports: ["8102:8102"]
    volumes:
      - ./shared:/app/shared
      - ./challenges/02-support-secrets:/app/challenge
    tmpfs: ["/tmp"]
    read_only: true
    security_opt: ["no-new-privileges:true"]
    cap_drop: ["ALL"]
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python3", "-c", "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8102/health').read()"]
      interval: 20s
      timeout: 5s
      retries: 3

  challenge03:
    build:
      context: .
      dockerfile: challenges/03-memory-lane/Dockerfile
    env_file: .env
    environment:
      PORT: "8103"
      DATA_DIR: /tmp/data
      LLM_PROVIDER: ${LLM_PROVIDER:-deepseek}
      CHALLENGE_FLAG: MSP{c0nv3rs4+i0n_his+0ry_b3c0m3s_4_+rus+_b0und4ry}
      ADMIN_TOKEN: ${ADMIN_TOKEN:-local-admin}
      RATE_LIMIT_PER_MINUTE: ${RATE_LIMIT_PER_MINUTE:-15}
      RELOAD: ${RELOAD:-true}
      PYTHONPATH: /app/challenge:/app
    ports: ["8103:8103"]
    volumes:
      - ./shared:/app/shared
      - ./challenges/03-memory-lane:/app/challenge
    tmpfs: ["/tmp"]
    read_only: true
    security_opt: ["no-new-privileges:true"]
    cap_drop: ["ALL"]
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python3", "-c", "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8103/health').read()"]
      interval: 20s
      timeout: 5s
      retries: 3

  challenge04:
    build:
      context: .
      dockerfile: challenges/04-docu-scan/Dockerfile
    env_file: .env
    environment:
      PORT: "8104"
      DATA_DIR: /tmp/data
      LLM_PROVIDER: ${LLM_PROVIDER:-deepseek}
      CHALLENGE_FLAG: MSP{34sy_LLM_P01s0n1ng}
      ADMIN_TOKEN: ${ADMIN_TOKEN:-local-admin}
      RATE_LIMIT_PER_MINUTE: ${RATE_LIMIT_PER_MINUTE:-12}
      RELOAD: ${RELOAD:-true}
      PYTHONPATH: /app/challenge:/app
    ports: ["8104:8104"]
    volumes:
      - ./shared:/app/shared
      - ./challenges/04-docu-scan:/app/challenge
    tmpfs: ["/tmp"]
    read_only: true
    security_opt: ["no-new-privileges:true"]
    cap_drop: ["ALL"]
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python3", "-c", "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8104/health').read()"]
      interval: 20s
      timeout: 5s
      retries: 3

  challenge05:
    build:
      context: .
      dockerfile: challenges/05-orderly/Dockerfile
    env_file: .env
    environment:
      PORT: "8105"
      DATA_DIR: /tmp/data
      LLM_PROVIDER: ${LLM_PROVIDER:-deepseek}
      CHALLENGE_FLAG: MSP{t00l_4bus3_5ucc3ss}
      ADMIN_TOKEN: ${ADMIN_TOKEN:-local-admin}
      RATE_LIMIT_PER_MINUTE: ${RATE_LIMIT_PER_MINUTE:-14}
      RELOAD: ${RELOAD:-true}
      PYTHONPATH: /app/challenge:/app
    ports: ["8105:8105"]
    volumes:
      - ./shared:/app/shared
      - ./challenges/05-orderly:/app/challenge
    tmpfs: ["/tmp"]
    read_only: true
    security_opt: ["no-new-privileges:true"]
    cap_drop: ["ALL"]
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python3", "-c", "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8105/health').read()"]
      interval: 20s
      timeout: 5s
      retries: 3

  challenge06:
    build:
      context: .
      dockerfile: challenges/06-nimbus-hr/Dockerfile
    env_file: .env
    environment:
      PORT: "8106"
      DATA_DIR: /tmp/data
      LLM_PROVIDER: ${LLM_PROVIDER:-deepseek}
      CHALLENGE_FLAG: MSP{retrieval_needs_provenance_not_hope}
      ADMIN_TOKEN: ${ADMIN_TOKEN:-local-admin}
      RATE_LIMIT_PER_MINUTE: ${RATE_LIMIT_PER_MINUTE:-12}
      RELOAD: ${RELOAD:-true}
      PYTHONPATH: /app/challenge:/app
    ports: ["8106:8106"]
    volumes:
      - ./shared:/app/shared
      - ./challenges/06-nimbus-hr:/app/challenge
    tmpfs: ["/tmp"]
    read_only: true
    security_opt: ["no-new-privileges:true"]
    cap_drop: ["ALL"]
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python3", "-c", "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8106/health').read()"]
      interval: 20s
      timeout: 5s
      retries: 3

  challenge07:
    build:
      context: .
      dockerfile: challenges/07-helphub/Dockerfile
    env_file: .env
    environment:
      PORT: "8107"
      DATA_DIR: /tmp/data
      LLM_PROVIDER: ${LLM_PROVIDER:-deepseek}
      CHALLENGE_FLAG: MSP{stored_input_can_cross_roles_and_workflows}
      ADMIN_TOKEN: ${ADMIN_TOKEN:-local-admin}
      RATE_LIMIT_PER_MINUTE: ${RATE_LIMIT_PER_MINUTE:-12}
      RELOAD: ${RELOAD:-true}
      PYTHONPATH: /app/challenge:/app
    ports: ["8107:8107"]
    volumes:
      - ./shared:/app/shared
      - ./challenges/07-helphub:/app/challenge
    tmpfs: ["/tmp"]
    read_only: true
    security_opt: ["no-new-privileges:true"]
    cap_drop: ["ALL"]
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python3", "-c", "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8107/health').read()"]
      interval: 20s
      timeout: 5s
      retries: 3

  challenge08:
    build:
      context: .
      dockerfile: challenges/08-atlas-analytics/Dockerfile
    env_file: .env
    environment:
      PORT: "8108"
      DATA_DIR: /tmp/data
      LLM_PROVIDER: ${LLM_PROVIDER:-deepseek}
      CHALLENGE_FLAG: MSP{ranking_is_a_security_control_when_trust_is_mixed}
      ADMIN_TOKEN: ${ADMIN_TOKEN:-local-admin}
      RATE_LIMIT_PER_MINUTE: ${RATE_LIMIT_PER_MINUTE:-12}
      RELOAD: ${RELOAD:-true}
      PYTHONPATH: /app/challenge:/app
    ports: ["8108:8108"]
    volumes:
      - ./shared:/app/shared
      - ./challenges/08-atlas-analytics:/app/challenge
    tmpfs: ["/tmp"]
    read_only: true
    security_opt: ["no-new-privileges:true"]
    cap_drop: ["ALL"]
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python3", "-c", "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8108/health').read()"]
      interval: 20s
      timeout: 5s
      retries: 3

  challenge09:
    build:
      context: .
      dockerfile: challenges/09-patchpanel/Dockerfile
    env_file: .env
    environment:
      PORT: "8109"
      DATA_DIR: /tmp/data
      LLM_PROVIDER: ${LLM_PROVIDER:-deepseek}
      CHALLENGE_FLAG: MSP{safe_tools_can_still_chain_into_unsafe_outcomes}
      ADMIN_TOKEN: ${ADMIN_TOKEN:-local-admin}
      RATE_LIMIT_PER_MINUTE: ${RATE_LIMIT_PER_MINUTE:-12}
      RELOAD: ${RELOAD:-true}
      PYTHONPATH: /app/challenge:/app
    ports: ["8109:8109"]
    volumes:
      - ./shared:/app/shared
      - ./challenges/09-patchpanel:/app/challenge
    tmpfs: ["/tmp"]
    read_only: true
    security_opt: ["no-new-privileges:true"]
    cap_drop: ["ALL"]
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python3", "-c", "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8109/health').read()"]
      interval: 20s
      timeout: 5s
      retries: 3

  challenge10:
    build:
      context: .
      dockerfile: challenges/10-meridian-research/Dockerfile
    env_file: .env
    environment:
      PORT: "8110"
      DATA_DIR: /tmp/data
      LLM_PROVIDER: ${LLM_PROVIDER:-deepseek}
      CHALLENGE_FLAG: MSP{retrieval_poisoning_plus_tool_trust_breaks_workflows}
      ADMIN_TOKEN: ${ADMIN_TOKEN:-local-admin}
      RATE_LIMIT_PER_MINUTE: ${RATE_LIMIT_PER_MINUTE:-10}
      RELOAD: ${RELOAD:-true}
      PYTHONPATH: /app/challenge:/app
    ports: ["8110:8110"]
    volumes:
      - ./shared:/app/shared
      - ./challenges/10-meridian-research:/app/challenge
    tmpfs: ["/tmp"]
    read_only: true
    security_opt: ["no-new-privileges:true"]
    cap_drop: ["ALL"]
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python3", "-c", "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8110/health').read()"]
      interval: 20s
      timeout: 5s
      retries: 3

  challenge11:
    build:
      context: .
      dockerfile: challenges/11-travel-desk-V2/Dockerfile
    env_file: .env
    environment:
      PORT: "8111"
      HOST: "0.0.0.0"
      DATA_DIR: /tmp/data
      LLM_PROVIDER: ${LLM_PROVIDER:-deepseek}
      CHALLENGE_FLAG: MSP{Unb3li3v4bl3_Pr0mp+_Inj3c+i0n_M4s+3r}
      ADMIN_TOKEN: ${ADMIN_TOKEN:-local-admin}
      RATE_LIMIT_PER_MINUTE: ${RATE_LIMIT_PER_MINUTE:-10}
      RELOAD: ${RELOAD:-true}
      PYTHONPATH: /app/challenge:/app
    ports: ["8111:8111"]
    volumes:
      - ./shared:/app/shared
      - ./challenges/11-travel-desk-V2:/app/challenge
    tmpfs: ["/tmp"]
    read_only: true
    security_opt: ["no-new-privileges:true"]
    cap_drop: ["ALL"]
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python3", "-c", "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8111/health').read()"]
      interval: 20s
      timeout: 5s
      retries: 3